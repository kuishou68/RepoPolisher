# RepoPolisher

🔍 一款自动化代码质量分析工具，专注于发现 GitHub 热门项目中的拼写错误、代码规范问题，并自动生成 PR 提交修复方案。

[English](./README.md) | [日本語](./README.ja.md) | [한국어](./README.ko.md)

## 项目愿景

RepoPolisher 旨在成为开源社区的"代码抛光师"：

1. **搜集热门项目** - 自动发现 GitHub 上 Star 数高、提交活跃、贡献者众多的优质项目
2. **智能分类展示** - 对项目进行分类，重点关注 AI 方向项目
3. **深度代码分析** - 扫描源码中的拼写错误、语法问题、设计缺陷等
4. **一键提交 PR** - 将修复方案整理成 PR，用户可选择性提交到原仓库

## 当前阶段

**Phase 1: 拼写错误修复** ✅ 已实现

- [x] GitHub 热门项目采集
- [x] 本地项目导入
- [x] 拼写错误检测（基于 cspell）
- [x] 差异预览与修复建议
- [x] PR 草稿创建与提交
- [x] GitHub CLI 集成

## 架构设计

```
RepoPolisher/
├── apps/
│   ├── desktop/          # Electron 桌面应用 (主入口)
│   │   ├── electron/     # 主进程
│   │   │   ├── main.ts   # Electron 主进程入口
│   │   │   ├── preload.ts
│   │   │   └── ipc/      # tRPC IPC 通信层
│   │   │       ├── router.ts    # 路由聚合
│   │   │       ├── projects.ts  # 项目管理
│   │   │       ├── analysis.ts  # 分析任务
│   │   │       ├── pr.ts        # PR 提交
│   │   │       └── settings.ts  # 设置管理
│   │   └── src/          # 渲染进程 (React)
│   │       ├── pages/    # 页面组件
│   │       │   ├── Projects/   # 项目列表
│   │       │   ├── Analysis/   # 分析详情
│   │       │   ├── PRs/        # PR 管理
│   │       │   └── Settings/   # 设置
│   │       └── components/
│   └── cli/              # CLI 工具 (规划中)
│
├── packages/
│   ├── core/             # 核心业务逻辑
│   │   ├── collector/    # 项目采集器
│   │   │   ├── github-api.ts    # GitHub API 集成
│   │   │   └── local-scanner.ts # 本地项目扫描
│   │   ├── analyzer/     # 代码分析器
│   │   │   └── typo-checker.ts  # 拼写检查 (cspell)
│   │   └── submitter/    # PR 提交器
│   │       ├── gh-cli.ts        # GitHub CLI 集成
│   │       └── auth-checker.ts  # 认证检测
│   │
│   ├── protocol/         # 工具协议层 (MCP/ACP 风格)
│   │   ├── types/
│   │   │   ├── tool.ts   # Tool 接口定义
│   │   │   └── event.ts  # 事件类型
│   │   ├── bus/
│   │   │   └── event-bus.ts     # 事件总线
│   │   └── registry/
│   │       └── tool-registry.ts # 工具注册表
│   │
│   ├── shared/           # 共享类型与工具
│   │   └── types/
│   │       ├── project.ts   # 项目类型
│   │       ├── analysis.ts  # 分析类型
│   │       └── pr.ts        # PR 类型
│   │
│   └── db/               # 数据持久化
│       └── schema.ts     # SQLite Schema (Drizzle ORM)
```

## 核心模块说明

### 1. Protocol 层 - 工具协议

借鉴 MCP (Model Context Protocol) 设计思想，提供统一的工具接口：

```typescript
interface Tool<TInput, TOutput> {
  name: string;
  version: string;
  category: 'collector' | 'analyzer' | 'generator' | 'submitter';
  inputSchema: JSONSchema;
  outputSchema: JSONSchema;
  execute: (input: TInput, context: ToolContext) => Promise<ToolResult<TOutput>>;
}
```

**设计优势**：
- 统一的工具定义格式，方便用户自行对接 Agent 和 Tools
- 事件驱动架构，各模块解耦
- 可扩展的插件系统

### 2. Core 层 - 业务逻辑

| 模块 | 功能 | 当前实现 |
|------|------|----------|
| `collector/github-api` | GitHub 热门项目采集 | ✅ Trending API + GraphQL |
| `collector/local-scanner` | 本地项目扫描 | ✅ Git 仓库检测 |
| `analyzer/typo-checker` | 拼写错误检测 | ✅ cspell 集成 |
| `submitter/gh-cli` | PR 提交 | ✅ GitHub CLI |

### 3. 数据模型

```
projects          # 项目信息
    ├── source (github/local)
    ├── github_* (GitHub 元数据)
    └── local_* (本地项目信息)

analysis_tasks    # 分析任务
    ├── type (typo/lint/ai)
    ├── status (pending/running/completed/failed)
    └── progress

issues            # 发现的问题
    ├── type, severity
    ├── original → suggestion
    └── status (open/included/ignored/fixed)

pr_drafts         # PR 草稿
    ├── title, body, branch
    ├── issue_ids[]
    └── status (draft/ready/submitted/merged)
```

## 用户交互流程

```
┌─────────────────────────────────────────────────────────────┐
│                    RepoPolisher 桌面应用                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ① 项目发现                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  [获取热门项目]  [添加本地项目]                        │   │
│  │                                                     │   │
│  │  📦 langchain (⭐ 95k) - AI                         │   │
│  │  📦 ollama (⭐ 80k) - AI                            │   │
│  │  📦 your-local-project - Local                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↓                                 │
│  ② 代码分析                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  [开始分析] ████████████░░ 75%                       │   │
│  │                                                     │   │
│  │  🔴 typo: "recieve" → "receive" (config.ts:42)      │   │
│  │  🟡 typo: "occured" → "occurred" (utils.ts:18)      │   │
│  │  ☑️ [选择] [预览差异] [忽略]                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↓                                 │
│  ③ PR 提交                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📝 PR 草稿: fix: correct 5 typos in codebase       │   │
│  │                                                     │   │
│  │  [预览 PR]  [编辑内容]  [提交到 GitHub]              │   │
│  │                                                     │   │
│  │  ⚠️ 检测到 gh-cli 已安装，将使用本地提交             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 分阶段路线图

### Phase 1: 拼写错误修复 ✅
- 基础架构搭建
- GitHub/本地项目导入
- cspell 拼写检查集成
- PR 提交流程

### Phase 2: 代码规范检测 🚧
- ESLint/Prettier 集成
- 多语言支持 (Python, Go, Rust)
- 自定义规则配置

### Phase 3: AI 深度分析 📋
- 设计缺陷检测
- 代码简化建议
- 公共逻辑抽离推荐
- 设计模式优化

### Phase 4: 插件生态 📋
- 完善 MCP/ACP 协议实现
- 用户自定义 Tool 开发
- Agent 对接接口
- 社区插件市场

## 快速开始

### 环境要求

- Node.js >= 18
- pnpm >= 8.10.0
- GitHub CLI (可选，用于提交 PR)

### 安装与运行

```bash
# 克隆仓库
git clone https://github.com/kuishou68/RepoPolisher.git
cd RepoPolisher

# 安装依赖
pnpm install

# 启动开发模式
pnpm dev
```

### GitHub CLI 配置（推荐）

```bash
# 安装 gh
brew install gh  # macOS
# 或 winget install GitHub.cli  # Windows

# 登录
gh auth login
```

## 技术栈

| 层级 | 技术选型 |
|------|----------|
| 桌面框架 | Electron 28 |
| 前端 | React 18 + TypeScript |
| 样式 | TailwindCSS + Radix UI |
| 状态 | Zustand + React Query |
| 通信 | tRPC + electron-trpc |
| 数据库 | SQLite + Drizzle ORM |
| 构建 | Vite + Turbo |
| 代码分析 | cspell (拼写检查) |

## 贡献指南

欢迎贡献！请查看 [Contributing Guide](CONTRIBUTING.md)。

## 许可证

MIT License

---

**让每一次提交，都成为开源社区的一份贡献 ✨**
